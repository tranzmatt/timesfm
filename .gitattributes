# Binary outputs in the timesfm-forecasting skill — no diff/merge munging.
# (Not stored in Git LFS: the repo has no LFS objects and these files are small.)
timesfm-forecasting/**/*.png binary
timesfm-forecasting/**/*.gif binary
